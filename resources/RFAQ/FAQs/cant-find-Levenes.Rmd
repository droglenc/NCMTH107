---
layout: page
author: Derek H. Ogle
title: MTH107 FAQ
subtitle: Why can't R find leveneTest()?
---

```{r echo=FALSE, eval=FALSE}
# Renders an appropriate HTML file for the webpage
setwd("C:/aaaWork/Web/GitHub/NCMTH107/resources/RFAQ/FAQs")
source("../../../rhelpers/rhelpers.R")
fnm <- "cant-find-Levenes"
modHTML(fnm)
```

```{r echo=FALSE, results='hide', message='FALSE'}
source("../../../rhelpers/knitr_setup.R")
```

----

The function provided in `NCStats` is `levenesTest()` (note the `s` on `levenes`) rather than the `leveneTest()` provided by the `car` package.  So, make sure to include the `s`.
